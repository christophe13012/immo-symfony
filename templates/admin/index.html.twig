{% extends "layout.html.twig" %}

{% block title %}Admin
{% endblock %}

{% block body %}
	<div class="container mt-4">
		<h2>Gérer les biens</h2>
		<table class="table table-dark">
			<thead>
				<tr>
					<th scope="col">#</th>
					<th scope="col">Titre</th>
					<th scope="col">Action</th>
				</tr>
			</thead>
			<tbody>
				{% for property in properties %}
					<tr>
						<th scope="row">{{property.id}}</th>
						<td>{{property.title}}</td>
						<td>
							<a href="{{ path('admin.edit', {'id': property.id}) }}" class="btn btn-success">Editer</a>
							<a href="{{ path('admin.edit', {'id': property.id}) }}" class="btn btn-danger ml-3">Supprimer</a>

						</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
	</div>
{% endblock %}
